# Documentation

- **overview/** — Project description, high-level explanation, CV/LinkedIn summary.
- **architecture/** — MRTA visualization, multi-drone control, ROS 2 topics.
- **missions/** — Scout mission and satellite scout (parameters, usage).
- **sensors/** — Camera topics and setup.
- **legacy/** — Old MRTA baseline and backups (for reference only).

Start with `overview/` if you want to understand what the project does; use `missions/` and `architecture/` when running or extending the system.
